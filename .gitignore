# Binaries
/mibeehive
mibeehive-arm64
/migrate
migrate-arm64
*.exe

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Runtime data
data/
*.db

# TLS certificates (auto-generated)
configs/server.crt
configs/server.key

# Sisyphus work directory
.sisyphus/
.playwright-mcp/

# Playwright
tests/
node_modules/
package-lock.json

# Generated
AGENTS.md

# QA Reports
docs/qa-report/

# Migrations (legacy duplicate of internal/db/migrations/).
# NOTE: the leading slash anchors this to the repo root ONLY. Without it, the
# unanchored `migrations/` pattern also matched internal/db/migrations/ and
# silently ignored newly added migration files (e.g. 021 was not committable).
/migrations/

# TLS/SSL certificate files
*.crt
*.key
*.pem

# Log files
*.log
